// import viajes from '../viajes.json'

export function ConsolaForEach(){
    // const arr = viajes
    // console.log('Cargando consola for each', arr)

    // arr.forEach(function(element, index) {
    //     console.log(element.destino, index)
    // })
    
    return(
        <div style={{border: '1px solid grey'}} className="componente">
            ConsolaForEach
        </div>
    )
}